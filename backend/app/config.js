const SECONDS = 1000;
const MINUTES = 60 * SECONDS;
const HOURS = 60 * MINUTES;
const DAYS = 24 * HOURS;

const REFRESH_RATE = 5; // units
const STARTING_BALANCE = 50;

module.exports = {
  SECONDS,
  MINUTES,
  HOURS,
  DAYS,
  REFRESH_RATE,
  STARTING_BALANCE
};
